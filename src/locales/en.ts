// English UI strings. Keys are added per phase. English is the fallback
// language, so any key missing from de.ts automatically shows English.
const en = {} as Record<string, unknown>;
export default en;
